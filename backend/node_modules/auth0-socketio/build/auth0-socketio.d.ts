import type { JWTPayload, JWTHeaderParameters } from 'jose';
import type { Socket as Socket } from 'socket.io';
declare module 'socket.io' {
    interface Socket {
        auth?: {
            user: JWTPayload;
            header: JWTHeaderParameters;
        };
    }
}
declare type SocketIOMiddlewareFactory = (domain?: string, audience?: string) => (socket: Socket, next: (err?: Error) => void) => void;
declare const auth0Middleware: SocketIOMiddlewareFactory;
export { auth0Middleware };
